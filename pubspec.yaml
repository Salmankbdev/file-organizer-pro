name: file_organizer_pro
description: "Scan, understand, and organize files on your desktop — fully offline."
publish_to: 'none'
version: 1.2.1+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  sqflite_common_ffi: ^2.3.4
  path: ^1.9.0
  path_provider: ^2.1.5
  file_picker: ^8.1.6
  shared_preferences: ^2.3.4
  crypto: ^3.0.6
  intl: ^0.20.2
  archive: ^4.0.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
